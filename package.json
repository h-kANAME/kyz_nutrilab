{
  "name": "kyz-nutrilab",
  "private": true,
  "version": "1.0.0",
  "description": "KYZ NutriLab — food tracking with Google auth and AI assist",
  "workspaces": [
    "apps/api",
    "apps/web"
  ],
  "scripts": {
    "dev:api": "npm run dev -w apps/api",
    "dev:web": "npm run dev -w apps/web",
    "build": "npm run build -w apps/api && npm run build -w apps/web",
    "start:api": "npm run start -w apps/api"
  },
  "engines": {
    "node": ">=20"
  }
}
